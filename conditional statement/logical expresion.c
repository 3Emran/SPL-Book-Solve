#include<stdio.h>
int main()
{
    float radius,area,circle,length,width;
    if(circle){
        scanf("%f",&radius);
        area = 3.1416*radius*radius;
        printf("Area of circle: %.4f",area);
   }


   else {
    scanf("%f %f",&length,&width);
    area = length*width;
    printf("Area of rectangle: %.2f",area);
   }

   return 0;
}
